services:
  db:
    image: postgres:17
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: dev
      POSTGRES_PASSWORD: dev
      POSTGRES_DB: project_base
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
